# Shared secret the Next.js server must send as X-Engine-Key
ENGINE_API_KEY=

# Port (default 8000)
ENGINE_PORT=8000

# Work root for temporary in/out PDFs (created and cleaned automatically)
# ENGINE_WORK_ROOT=/var/tmp/editor-engine-work
